apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ include "catalog.fullname" . }}-secretstore
  namespace: {{ .Release.Namespace }}
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.aws.region }}