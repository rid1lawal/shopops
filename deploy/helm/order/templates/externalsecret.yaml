apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "order.fullname" . }}-db
spec:
  refreshInterval: 1h

  secretStoreRef:
    name: {{ include "order.fullname" . }}-secretstore
    kind: SecretStore

  target:
    name: {{ .Values.database.existingSecret }}
    creationPolicy: Owner

  data:
    - secretKey: {{ .Values.database.existingSecretKey }}
      remoteRef:
        key: shopops/order
        property: DB_URL